<div class="p-6 max-w-2xl mx-auto">
  <h1 class="text-3xl font-bold mb-6">Edit collaborator</h1>
  <%= render "form", collaborator: @collaborator %>
  <div class="mt-4">
    <%= link_to "Back", collaborator_path(@collaborator), class: "text-blue-600 hover:text-blue-800" %>
  </div>
</div>
