<div class="p-6">
  <% if notice %>
  <p class="mb-4 p-4 bg-green-100 text-green-700 rounded"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold">Teachers</h1>
  </div>

  <div class="overflow-x-auto shadow-md rounded-lg">
    <table class="w-full border-collapse">
      <thead class="bg-gray-100 border-b">
        <tr>
          <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">First name</th>
          <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Last name</th>
          <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Email</th>
          <th class="px-6 py-3 text-left text-sm font-semibold text-gray-700">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @teachers.each do |teacher| %>
        <tr class="border-b hover:bg-gray-50">
          <td class="px-6 py-4 text-sm text-gray-900"><%= teacher.firstname %></td>
          <td class="px-6 py-4 text-sm text-gray-900"><%= teacher.lastname %></td>
          <td class="px-6 py-4 text-sm text-gray-900"><%= teacher.email %></td>
          <td class="px-6 py-4 text-sm space-x-2">
            <%= link_to "Show", teacher_path(teacher), class: "text-blue-600 hover:text-blue-800" %>
            <% if current_person.is_a?(Dean) %>
            <%= link_to "Edit", edit_teacher_path(teacher), class: "text-yellow-600 hover:text-yellow-800" %>
            <%= link_to "Destroy", teacher_path(teacher), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "text-red-600 hover:text-red-800" %>
            <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>