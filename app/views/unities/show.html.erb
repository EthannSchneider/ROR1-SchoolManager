<div class="p-6">
  <% if notice %>
  <p class="mb-4 p-4 bg-green-100 text-green-700 rounded"><%= notice %></p>
  <% end %>

  <div class="mb-6">
    <%= link_to "← Back to module", formation_plan_formation_module_path(@formation_plan, @formation_module), class: "text-blue-600 hover:text-blue-800" %>
  </div>

  <div class="bg-white shadow-md rounded-lg p-6">
    <div class="flex justify-between items-start mb-6">
      <h1 class="text-3xl font-bold text-gray-900"><%= @unity.name %></h1>
      <% if current_person.is_a?(Dean) %>
      <div class="flex gap-2">
        <%= link_to "Edit", edit_formation_plan_formation_module_unity_path(@formation_plan, @formation_module, @unity), class: "px-4 py-2 bg-yellow-600 text-white rounded hover:bg-yellow-700" %>
        <%= link_to "Destroy", formation_plan_formation_module_unity_path(@formation_plan, @formation_module, @unity), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700" %>
      </div>
      <% end %>
    </div>

    <div class="mt-6">
      <h2 class="text-xl font-semibold mb-4">Module</h2>
      <p class="text-gray-900">
        <%= link_to @formation_module.name, formation_plan_formation_module_path(@formation_plan, @formation_module), class: "text-blue-600 hover:text-blue-800" %>
      </p>
    </div>
  </div>
</div>